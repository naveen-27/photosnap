import React from "react";
import HeroContainer from "../HeroContainer";

const Home = () => {
  document.title = "Photosnap - Home";

  return (
    <>
      <HeroContainer />
    </>
  );
};

export default Home;
